import React, { Fragment } from "react";
import Hero from "../components/hero/Hero";

const HeroPage = () => {
  return (
    <Fragment>
      <Hero />
    </Fragment>
  );
};

export default HeroPage;
